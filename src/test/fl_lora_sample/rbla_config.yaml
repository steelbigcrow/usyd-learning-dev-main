# yaml files path
yaml_section_path: "./yamls/"

# yaml files
yaml_section_files: 
  # <file name> : <name>, if name is empty use file name without extension
  - "section-general.yaml" : general
  - "section-training_logger.yaml" : training_logger
  - "section-aggregation-rbla.yaml": aggregation
  - "section-client_selection.yaml": client_selection
  - "section-dataset_loader-mnist.yaml" : dataset_loader_mnist
  - "section-dataset_loader-cifar10.yaml" : dataset_loader_cifar10
  - "section-data_distribution-cifar10.yaml" : cifar10_distribution
  - "section-data_distribution-mnist.yaml" : mnist_distribution
  - "section-loss_func.yaml" : loss_func
  - "section-lora_inference.yaml" : lora
  - "section-nn_model-mnist-mlp.yaml" : nn_model_mnist_mlp_lora
  - "section-nn_model-cifar10-vit.yaml" : nn_model_cifar10_vit
  - "section-fl_nodes.yaml" : fl_nodes
  - "section-optimizer.yaml" : optimizer
  - "section-training.yaml" : training
  - "section-client_strategy-rbla.yaml" : client_strategy
  - "section-server_strategy-rbla.yaml" : server_strategy
  - "section-runner_strategy-rbla.yaml" : runner_strategy
  - "section-trainer-standard.yaml" : trainer_standard
  - "section-trainer-vit.yaml" : trainer_vit
  - "section-rank_distribution.yaml" : rank_distribution
#---------------------------------------
# Combine yamls, can define any combination name,
# such as 'runner', 'runner-test', 'runner-a' etc
#---------------------------------------
yaml_combination:
  # Runner
  runner: [general, training_logger, fl_nodes, runner_strategy]

  # Client
  client_yaml: [general, training_logger, dataset_loader_mnist, nn_model_mnist_mlp_lora, loss_func, optimizer, training, cifar10_distribution, client_strategy, trainer_standard] #, trainer

  # Edge
  edge_yaml: []  

  # Server
  # server_yaml: [general, training_logger, dataset_loader, nn_model, aggregation, client_selection, server_strategy, trainer]
  server_yaml: [general, training_logger, dataset_loader_mnist, nn_model_mnist_mlp_lora, loss_func, optimizer, training, cifar10_distribution, client_strategy, trainer_standard, client_selection, server_strategy, aggregation, rank_distribution, lora]

  # Other combinations
